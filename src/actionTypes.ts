enum ActionType {
  CREATE = 'CREATE', // create cache
  CREATED = 'CREATED', // created cache
  ACTIVE = 'ACTIVE', // active cache
  DESTROY = 'DESTROY' // destroy cache
}

export default ActionType;
